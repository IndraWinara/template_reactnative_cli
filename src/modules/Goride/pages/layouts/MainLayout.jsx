import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import Navigator from '../routes/Navigator';

const MainLayoutGoRide = ({children}) => {
  return (
    <>
      <Navigator />
    </>
  );
};

export default MainLayoutGoRide;

const styles = StyleSheet.create({});
